/* global appVarObj */
/* global ons */
appVarObj = {
    currentQueue : [],
    my_media : null,
    mediaTimer : null,
    mediaStatus : null
}
angular.module('Musplay.controllers', [])

    .controller('MainController', function ($scope) {
    })
